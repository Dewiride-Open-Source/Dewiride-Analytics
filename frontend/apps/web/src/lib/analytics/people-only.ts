/**
 * Whether the picture is kept to the people a website is for.
 *
 * Everything the website recorded is the honest whole, and it is what the picture draws unless
 * somebody asks otherwise. But the question most owners bring to it is how many people came, and
 * a machinery-heavy week buries that under a band three times its height. Asking for the people
 * alone changes what is drawn rather than how, so it travels in the address like the view does.
 */

/** What asking for people alone is called in an address. */
export const PEOPLE_KEY = 'only';

/**
 * Who the picture may be kept to.
 *
 * A closed set of one. The address is written by whoever sent the link, so a word that is not
 * this one leaves the picture drawing everybody.
 */
export const PEOPLE = ['people'] as const;
